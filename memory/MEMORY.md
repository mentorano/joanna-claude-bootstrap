- [Pre-implement gates](feedback_pre_implement_gates.md) — The 3 gates (Inventory / Persona value / Mental simulate) + Strategic + Half-baked tests. Single highest-leverage file. Procedural ritual in project root CLAUDE.md; this is the case-study calibration.
- [Persona doctrine](feedback_persona_doctrine.md) — Persona-friendly defaults, affordance restraint, terminology canon, mock-the-breadth. The tiebreaker for any UX micro-decision. Project supplies specific persona name and terminology.
- [Workflow](feedback_workflow.md) — Driver mode (chunk granularity), documentation reflex, chunk-end persona+reflection passes, verify scope against code not stale docs.
- [Standing mode + no silent caps](feedback_standing_mode_no_silent_caps.md) — Honor standing directives (ultracode) by default — don't drift to lighter mode, don't re-ask; reconcile built-vs-manifest before „done"; log every deferral, never silently cap scope.
- [Iteration killers](feedback_iteration_killers.md) — When already in iteration loop: ask for console+network, prototype UX variants, ask strategic question, stop half-baked compromises. Companion to pre-implement-gates.
- [Tooling](feedback_tooling.md) — Edit not sed, git -C not cd, venv binary not source, pytest/curl not python -c, visible Chrome not headless. Extended rationale; hard rules in project root CLAUDE.md.
- [Audit default](feedback_audit_default.md) — Value statement: anything forensically meaningful gets persisted. Audit-every-mutation is strictest application.
- [shadcn *Label primitives require Group ancestor](feedback_shadcn_grouplabel.md) — DropdownMenuLabel + analogous wrap Base UI GroupLabel; runtime crash without Group wrapper. Library gotcha.
- [Base UI Select.Value lazy fallback](feedback_baseui_select_value.md) — SelectValue shows raw value (not label) until SelectContent mounts. Use children-as-function. Library gotcha.
- [Base UI render prop (not asChild)](feedback_baseui_render_prop.md) — shadcn v4 uses Base UI; element substitution is `render={<X/>}`, not Radix `asChild`. For styled Link: use `buttonVariants` className. Library gotcha.
- [Base UI Dialog initial focus](feedback_baseui_dialog_initial_focus.md) — Sheet/Dialog auto-focuses first focusable on open (search input → drawer opens focused / mobile keyboard). `initialFocus={false}` focuses the popup instead. Base UI does NOT auto-focus on touch → desktop-only issue.
- [Sticky header + horizontal scroll](feedback_sticky_header_horizontal_scroll.md) — Coupled CSS gotchas: Tailwind preflight `border-collapse:collapse` breaks sticky `<th>` (use `border-separate`); an `overflow-x` ancestor captures vertical sticky → window-sticky impossible → give the table its own bounded `max-h`+`overflow-y` scroll zone; verify by measuring the `<th>` not the static `<thead>`.
- [Min-size overflow blowout](feedback_min_size_overflow_blowout.md) — flex/grid child has `min-width/height:auto` → a `1fr` track / flex child holding an overflow-scroll element (or long token) blows out the parent instead of scrolling. Fix: `min-w-0` / `min-h-0` on the track/child.
- [Responsive input gotchas](feedback_responsive_input_gotchas.md) — conditional `pr` so reserved clear-button space doesn't clip the placeholder; icon `top-1/2 -translate-y-1/2` breaks under asymmetric parent padding (wrap input in padding-free relative); input font <16px zooms on iOS.
- [Smoke + downstream display effects](feedback_smoke_test_downstream.md) — Mutation smoke must walk every surface that reads affected data, not just the mutation flow. Soft-delete: always test existing references. Companion to workflow chunk-end persona pass.
- [Permission symmetric admin for shared resources](feedback_permission_symmetric_admin.md) — Admin page for resource X where lower role already mutates via another flow: default to lower-role+ view+edit; restrict only destructive ops to admin.
- [Cross-impact reasoning](feedback_cross_impact_reasoning.md) — When changing a constraint/validator/lifecycle transition, enumerate ALL related paths (create/update/hide/unhide/archive/restore) in SAME chunk. Bugs from forgotten cross-impact cost more than the original change.
- [User-visible errors](feedback_user_visible_errors.md) — Every async user-triggered mutation needs explicit try/catch + toast/UI feedback. Never silent fail to console. Use `extractFriendlyError(err)` helper to surface Pydantic 422 / domain detail strings.
- [Cache invalidation cascade](feedback_cache_invalidation_cascade.md) — When mutation changes X, invalidate ALL queryKeys returning X-derived data — including embedded copies in other resources' responses, denormalized lifted tables, computed summaries. Sub-rule: the invalidated key must LITERALLY match the consuming query's key (copy from its `KEYS`); a plausible-but-wrong string fails silently.
- [Schema-driven, not hardcoded keys](feedback_schema_driven_not_hardcoded.md) — Behavior over config/schema-defined data must derive from the schema (field kind/renderer), never hardcode canonical/pilot keys. Hardcoded keys break for admin-added / second-tenant / renamed fields — and pilot tests never catch it (they match the fixture). Recurring across validators, import/export, display, search.
- [Defensive data-shape reads](feedback_defensive_data_shape.md) — Queries/parsers that assume a shape (JSON array, type, presence) must guard against violations — legacy rows, prior bugs, schema drift leave nonconforming data. One bad row 500s the whole endpoint (`jsonb_array_elements_text` on a scalar). Guard the read (`jsonb_typeof='array'`); fixing the write forward doesn't fix existing rows.
- [Theme-aware visual feedback](feedback_theme_aware_visual_feedback.md) — Visual cues must be tested against actual theme palette. „from-card to-transparent" is invisible on card-bg. Use inset shadow / theme-independent recipes.
- [Consistent save model](feedback_consistent_save_model.md) — Don't mix batched + per-action save in same surface. User settles into one model; mixed causes silent data loss.
- [UI state hygiene](feedback_ui_state_hygiene.md) — Auto-close after natural completion; wrap around serial nav; progressive disclosure for unhelpful affordances. Don't leave user in implicit states.
- [Confirm dialog for impactful saves](feedback_confirm_dialog_rule.md) — Every impactful/destructive save in admin/config surfaces goes through ConfirmDialog with before→after preview. Promise-based `useConfirm()` hook pattern.
- [Retro: rank by impact](feedback_retro_rank_by_impact.md) — When analyzing a session for RL items, lead with principle-level rules (promote to CLAUDE.md + bootstrap), not flat chronological list. User shouldn't have to pull principles out.
- [Memory birth → audit sweep](feedback_memory_birth_audit.md) — When you formalize a memory rule that names a bug class, IMMEDIATELY grep codebase for other instances of the same pattern. Don't wait for next bug. Memory without audit = N-1 instances still in code.
- [Prototype before deep build](feedback_prototype_before_deep_build.md) — For UX decisions where the right pattern is uncertain (save model, mode granularity, confirmation cadence), wire ONE control with the candidate pattern → validate → only then replicate across surface. Avoids sunk-cost refactors when wrong pattern picked.
- [Server-authoritative compute](feedback_server_authoritative_compute.md) — When client computes a value (max, count, suggestion, aggregation) from a paginated list, it's wrong on page 2+. Move compute server-side and denormalize into the response. Triggers: defaults/suggestions, aggregations, availability checks, uniqueness.
- [Should the constraint exist?](feedback_should_the_constraint_exist.md) — Преди да дизайнваш UX/код около ограничение, питай дали ОГРАНИЧЕНИЕТО въобще трябва да съществува. Implementation-level workarounds често не са product requirements. Caught 2026-05-20 (max-1-lookup_multi per register).
- [React identity churn on mutable keys](feedback_react_identity_churn.md) — `<X key={mutableValue}>` causes unmount/remount при промяна; focus loss + popover state wipe + draft loss. Use stable client-side ID separate от storage key. Caught многократно — last Digital Archives admin label-typing.
- [has_change early-return blindspot](feedback_has_change_early_return.md) — Update endpoint-и с `if not has_change: return` блейкват silently mutations на properties които не са в diff calculator. Добавя нов mutable field → audit early-returns. Cause на „save returns 200 but doesn't persist" мистериите.
- [Plural stem change](feedback_plural_stem_change.md) — БГ sg/pl често имат stem change (ё→е): „промяна" → „промени" (не „промяни"). Use full-word ternary вместо stem-suffix concat. Audit existing `${n===1?"":"и"}` patterns.
- [Iteration compounding](feedback_iteration_compounding.md) — **Highest-pain session pattern.** Refactor-on-refactor без clean checkpoint + declaring „fix" преди user-path verification → user retест-ва 3-4 пъти за едно logical change. Walk through entire flow (mental simulate + curl actual data) преди да declare готово. Commit checkpoint между refactor-ите.
- [Prevent iteration cycles](feedback_prevent_iteration_cycles.md) — **Upstream sibling на iteration_compounding.** Three prevention disciplines: (1) ground-truth user intent преди fix („кликна ли тук? имаш предвид Y или Z?"), (2) run own smoke (TS+tests+mental simulate+curl actual data) преди declare готово — не прави user твоя QA loop, (3) re-communicate scope при mid-chunk expansion (estimate х2 exceeded).
- [PR review: three-dot diff](feedback_pr_review_three_dot_diff.md) — For PR reviews ALWAYS use `git diff main...branch` или `gh pr diff <#>`, never `main..branch` (two-dot). Two-dot includes main's post-split additions as fake „deletions on branch side" → phantom regression alarms. Universal git lesson.
- [Search review query-input matrix](feedback_search_review_matrix.md) — Code review of search/parsing/„many input forms" features requires explicit query-input matrix from author + adversarial brainstorm (empty / punctuation / impossible IDs / informal aliases / unknown scopes). Verbal „smoke checked X" not sufficient.
- [Cursor pointer „bible" rule](feedback_cursor_pointer_rule.md) — Tailwind v4 + shadcn default to `cursor-default`. Project must compensate с (a) global CSS override on button/role=button/a[href]/summary/label[for] AND (b) `cursor-pointer` в Button cva variant. Two layers, не one. Audit on shadcn upgrades.
- [No screen-dim for onboarding](feedback_no_screen_dim.md) — Никакъв spotlight overlay, dim backdrop, или wizard tour за onboarding/secondary UI. Aggressive overlay treatments сигнализират error/blockage to users, not help. Anchor-targeted soft cues only (pulsing dot, callout chip).
- [Preserve layout invariants when iterating](feedback_layout_invariants_polish.md) — При polishing card/list content в chunk N+1, explicit verify че paired-height / h-full / className-forward / sticky positioning от предишни chunks остават intact. Recurring regression class — polish chunks tend да break each other.
- [UI pattern symmetry — audit siblings](feedback_ui_pattern_symmetry_catch.md) — При fix на UI pattern в едно място, proactively audit analogous instances в sibling sections и fix в same chunk. Не чакай user да каже „и това де".
- [Keyboard nav в popovers с mixed editors](feedback_keyboard_nav_popover_iterations.md) — Popovers със text input + date trigger + select + button require hybrid keyboard nav: cursor-aware за text, unconditional за triggers, escape rule за role=option/menuitem/gridcell. Pure „always navigate" и pure „cursor-edge only" both fail.
- [Archived status semantics](feedback_archived_status_semantics.md) — Soft-delete pattern: archived ≠ deleted. Releases identifiers (frees „taken" slots), preserves data for audit/restore, skipped from default UI, re-validates on unhide. Partial unique constraint („active only"). Universal soft-delete contract.
- [Rapid mutation — no invalidate](feedback_rapid_mutation_no_invalidate.md) — За drag-resize/slider/type-as-you-go mutations, drop `onSettled: invalidateQueries`. Refetch race-ва с in-progress local state и wipes user input. Optimistic + onError + onSuccess defensive sync.
- [HMR diagnosis — curl first](feedback_hmr_diagnosis_curl_first.md) — „Не виждам промяна" → curl served file FIRST преди да допускаш code bug. Multi-port Vite (worktrees) + browser cache causes 60% от false alarms. Specific 4-step diagnosis order.
- [Validation bounds — frontend ↔ backend match](feedback_validation_bounds_match.md) — Frontend min/max constraints (slider, drag floor, input) трябва match с backend Pydantic ge/le. Mismatch → 422 reject → silent rollback → „bounce back" perception. Cross-check SAME chunk.
- [Surface fundamental trade-offs](feedback_surface_fundamental_tradeoffs.md) — При user requests с internally contradictory constraints (X AND Y AND Z geometrically impossible), STOP serial workarounds → surface trade-off explicitly с options. „Не мога освен по този начин" е valid и често по-добре от 5-та итерация.
- [No foundation-only commits](feedback_no_foundation_only_commits.md) — Commits със само backend + hooks (без user-visible feature) → user пита „къде е?". Винаги включвай AT LEAST един vertical slice на UI per commit. „Infrastructure only" допустимо когато explicitly документирано.
- [Match referenced pattern closely](feedback_match_referenced_pattern.md) — Когато user референцира съществуващ pattern („като X"), match-вай го close-ly включително semantic details. Compromise за „простота" е OK САМО ако pre-implementation surface-неш trade-off-а и user OK-не.
- [Ambiguous scope — ask before](feedback_ambiguous_scope_ask_before.md) — „Per X" formulations с hierarchical interpretations (per-user-global vs per-(user, column)) → ASK преди да commit-неш дизайна. Refactor cascade-ва през schema + hooks + UI + cell apply.
- [Understand pushback before reverse](feedback_understand_pushback_before_reverse.md) — User pushback е симптом, не диагноза. Reverse immediately без understanding на underlying need → pendulum behavior. Paraphrase complaint + hypothesize need, surface trade-offs с options.
- [HTML5 DnD click guard](feedback_html5_dnd_click_guard.md) — Native DnD fires click AFTER drop. Headers с двойна функция (click → popover, drag → reorder) trigger-ват unintended click след drag. Guard с justDraggedRef + setTimeout(100).
- [PR-only workflow](feedback_pr_only_workflow.md) — ВСИЧКИ commits (код + docs) → feature branch → PR → merge. Никога `git push origin main` директно. Prerequisite за autonomous code review agents (working срещу `pull_request` events). Soft enforcement; harden чрез branch protection след confident period. **Owner override**: explicit „push directly" / „skip PR" instruction = highest priority, per-instruction (не persistent policy reversal).
- [Simplicity over completeness](feedback_simplicity_over_completeness.md) — When a more complete/honest/granular design risks confusing users, prefer the simpler model they understand. Test: does the CURRENT model confuse real users, not is it theoretically lossless. Gate richness behind progressive disclosure. Know when to STOP refining. (digital-archives search tags, 2026-06-10.)
